import problem01 from '../01_Report-Repair'

describe('Problem 01', () => {
  test(`answer is ${problem01.solve()}`, () => {
    /* ok */
  })
  xtest(`bonus answer is ${problem01.solveBonus()}`, () => {
    /* ok */
  })
})
